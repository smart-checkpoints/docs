---
title: "Introduction"
description: "Average speed enforcement over the real driving distance between camera checkpoints."
---

Smart Checkpoints measures how fast a vehicle crossed a stretch of road, not how
fast it was moving at the instant it passed a camera.

A spot camera samples speed at a single position. Drivers learn where it is,
brake for it, and accelerate past it. The reading is accurate and the road stays
unprotected. Smart Checkpoints models a city as a graph of camera checkpoints
and enforces the average speed across the real driving distance between them,
so there is no single point to slow down for.

## The shape of the system

<Columns cols={3}>
  <Card title="The server" icon="server" href="/reference/rest-api">
    Holds the graph, records sightings, and turns distance and elapsed time into
    violations.
  </Card>
  <Card title="Distance drivers" icon="route" href="/concepts/distance-drivers">
    Separate processes that answer one question: how far apart are these two
    checkpoints by road, in metres.
  </Card>
  <Card title="Checkpoints" icon="camera" href="/concepts/enforcement">
    Cameras that report a plate and a timestamp to the server over HTTP.
  </Card>
</Columns>

<Columns cols={2}>
  <Card title="The console" icon="table-columns" href="/reference/console">
    The operator surface the server ships with: the project list, the live
    checkpoint graph and map view, its data-quality diagnostics, and the project
    table.
  </Card>
  <Card title="The website" icon="globe" href="https://smartcheckpoints.xyz">
    What the system is and why it measures the road rather than the moment.
  </Card>
</Columns>

The server never guesses a road distance. It asks a driver, and if no driver
answers, the edge is left unmeasured rather than filled with a straight-line
estimate. An unmeasured edge is a visible gap. A plausible wrong distance is an
invisible one, and it produces violations that look exactly like real ones.

## Start here

<Columns cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Run the server, build a two checkpoint graph, and produce your first
    violation.
  </Card>
  <Card title="The graph model" icon="diagram-project" href="/concepts/graph-model">
    Projects, nodes, edges, and the arithmetic that decides a violation.
  </Card>
  <Card title="Data quality" icon="location-crosshairs" href="/concepts/data-quality">
    How the system finds a checkpoint that is in the wrong place, and how to move
    it once you have.
  </Card>
  <Card title="Distance driver protocol" icon="plug" href="/reference/driver-protocol">
    The WebSocket contract: coordinates in, metres out, geometry optional.
  </Card>
</Columns>

<Info>
  Smart Checkpoints is open source under the MIT license. The code lives in the
  [Smart Checkpoints organisation](https://github.com/smart-checkpoints) on
  GitHub.
</Info>
